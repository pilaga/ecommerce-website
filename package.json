{
  "name": "nodejs-server",
  "version": "1.0.0",
  "description": "basic nodejs server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "author": "Pierre",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
